# 安妮播放器融合版 · 更新日志

## V4.0.4（2026-09-24）

**工程**
- **am.js UI 层拆分**：Apple Music 主题 2448 行单 IIFE 按职责拆为 5 片——am.js（核心状态/数据层/播放控制）、am-stream.js（在线音乐/下载）、am-lyrics.js（歌词解析与逐字渲染）、am-dom.js（界面构建/专辑网格/曲目表窗口化）、am-render.js（状态刷新/沉浸/迷你/挂载）；共享闭包变量经内部模块桥（`__annieAMInternal`）传递，前向引用用运行时解析的转发桩
- 纯搬运零行为变更：切片拼接与原稿逐字节一致（脚本断言）+ 跨片引用审计（111 个顶层函数不重不漏）+ vm 沙箱加载烟测，dev 实例全功能手动验收通过

## V4.0.3（2026-09-22）

**修复**
- **PEQ 第 9–12 段静默失效**：UI 允许添加 12 段但引擎上限为 8 段，超出部分被引擎拒绝且前端静默吞错——UI 上限与文案统一为 8 段

**安全（自定义音源沙箱加固）**
- 音源脚本执行环境从 worker_threads 升级为**独立子进程**，并带 `--disallow-code-generation-from-strings` 启动旗标：进程级禁用 eval / Function 构造器，封堵「宿主函数 .constructor("return process")()」经典逃逸链（8 类逃逸向量实测全部阻断，见 scripts/sandbox-escape-test.js）
- 音源请求处理增加 15s 超时兜底（脚本返回永不 resolve 的 Promise 不再无限挂起）
- 新增沙箱端到端验证脚本（正常脚本可用 / 逃逸被阻断 / 挂死可熔断），已接入 CI 与发版双闸门
- 提示：沙箱以故障隔离与纵深防御为目标，Node vm 官方声明非安全边界——请只导入可信来源的音源脚本

**文档**
- README 口径统一：图示 EQ 15 段（32Hz–16kHz，引擎与 UI 一致）/ 参量 EQ 8 段；当前版本号与 Release 同步

## V4.0.2（2026-09-22）

**工程**
- **长稳测试脚本**（`npm run test:soak`）：引擎级循环播放混合采样率素材（普通播放/无缝切歌交替 + 定时 seek / 暂停恢复），按间隔采样 stats RPC + 进程内存/句柄/线程数，结束自动出 Markdown 报告（崩溃/解码失败判失败，内存增长>30% 判疑似泄漏，欠载增长提示）；WASAPI 共享低音量运行不抢声卡，Ctrl+C 提前结束照样出报告
- **曲库性能 Benchmark**（`npm run bench:library`）：真实文件层（1k/10k，--full 加 50k：目录遍历+拼音排序+元数据解析实测）+ 数据层（1k~20 万合成：启动加载/搜索/内存），排序直接执行生产 listWorker.js 代码，结果写入 GPT 建议的对照表

## V4.0.1（2026-09-22）

**新功能**
- **链路图展开版**：设置中心 → 音频输出 → 输出健康，点击音质链路图弹出「全参数对照」浮层——源（编码/位深/采样率/声道）→ 解码 → VST（逐个插件名+耗时）→ 图示 EQ（非零段增益）→ PEQ（逐段 f/g/Q）→ 声道矩阵（模式/平衡）→ 响度（实际增益 dB）→ 前级限幅（补偿量/限幅器）→ 重采样（比率/质量档）→ 输出（设备/格式/独占/缓冲/切歌方式）→ 运行健康（欠载/限幅计数），每段标注「生效/直通」，底部给出 Bit-perfect 结论与破坏原因

## V4.0.0（2026-09-22）

**V4 · Audio Core & Reliability**：功能冻结，专注音频内核可靠性与架构。

**工程**
- **引擎核心模块化拆分**：Engine.cs（1518 行单文件）按职责拆为四个 partial class——Engine.cs（RPC 分发/设备选择/统计）、Engine.Dsp.cs（EQ/PEQ/声道矩阵/防削波链）、Engine.Vst.cs（VST3 效果器链与原生编辑器）、Engine.Playback.cs（播放状态机/DoP/交叉淡入/进度上报）。纯搬运零行为变更，27 项音频正确性测试全部通过
- 顺手清除一处死代码（无调用的 ProbeGuarded）

## V3.5.19（2026-09-22）

**新功能**
- **声道工具箱**：设置中心 → 播放新增 左右平衡（双击回中）/ 左右互换 / 单声道合并 / 左/右声道反相——引擎 2x2 声道矩阵实时处理，耳机党与音箱摆位刚需
- **参量均衡器（PEQ）**：自由频段 peaking biquad（频率/增益/Q 全自定义，最多 12 段），与 15 段 EQ 串联；耳机校准方案（AutoEq 等）逐条添加，正增益自动计入前级防削波
- **音质链路图**：输出健康卡片新增链路可视化——源 → VST → EQ → PEQ → 声道 → 响度 → 前级限幅 → 重采样 → 输出，激活段点亮、直通段灰显
- **智能歌单生成器**：曲库工具新增——按艺术家/专辑/流派/播放次数/最近播放/仅无损组合筛选，实时预览匹配数，一键生成并播放或存为播放列表（三主题共享）
- **每日推荐（本地版）**：智能列表新增「✨ 每日推荐」——基于听歌统计的艺术家偏好 60% + 随机探索 40%，排除今天已播，完全离线，每天一份
- **收听热力图**：听歌报告新增近半年 GitHub 风格热力图（悬停看每日时长，跟随强调色）
- **网易云热门评论**：AM 沉浸页/迷你 ☰ 面板新增「评论」页签；网易曲目直取热评，本地曲目自动按歌名+歌手匹配

## V3.5.18（2026-09-22）

**新功能**
- **系统媒体控制（SMTC）**：键盘媒体键 ⏯⏮⏭ 全局控制播放；Windows 音量浮层/锁屏显示封面与曲目信息，可直接拖进度条
- **任务栏进度条**：播放进度实时显示在任务栏图标上（暂停变暗、停止清除）
- **氛围模式（全屏频谱）**：全屏 32 频段实时频谱 + 当前曲目信息，跟随强调色；AM 主题点击底部频谱条 / Ctrl+Shift+V / 设置中心 → 常规 进入，ESC 或单击退出
- **听歌报告分享图**：报告浮层新增「生成分享图」——canvas 绘制精美卡片直接复制到剪贴板，群里 Ctrl+V 即晒
- **设置中心「使用说明」选项卡**：说明书应用内版，覆盖三大界面每个按钮与全部功能；每条进设置搜索索引，随版本同步更新

## V3.5.17（2026-09-22）

**新功能**
- **更新播报**：在线更新后首次启动自动弹出"本版新变化"（读取更新日志当前版本段落），不再更完不知道更了啥
- **听歌报告**：设置中心 → 曲库工具新增「查看报告」——本地统计 Top 歌曲/艺术家/专辑、累计收听时长、最爱听歌时段，一键复制文本分享（数据仅存本机）
- **主题强调色自定义**：设置中心 → 常规新增色板（默认金/珊瑚红/落日橙/香槟金/翡翠绿/天际蓝/罗兰紫），三套界面同步换色
- **AM 可视化频谱条**：AM 主题底部新增 32 频段实时频谱（引擎端 FFT 分析，跟随强调色），设置中心 → 常规可关
- **复制诊断摘要**：设置中心 → 曲库工具新增一键复制（版本/系统/引擎/输出设备/曲库规模），群里报障不用导 zip 直接粘贴

**工程**
- CI 日常检查工作流：main 分支推送/PR 自动跑全量 JS 语法检查 + 引擎编译检查，语法错误进不了主干（`node scripts/check-syntax.js` 本地可跑）

## V3.5.16（2026-09-22）

**修复**
- **引擎在卡巴斯基等安全软件环境下启动即崩**（0xC0000602 fast-fail，反复重启无法播放）：杀软注入 DLL 投递 APC 与 .NET 默认开启的 CET 影子栈在老版 Win10 上冲突，引擎已不再标记 CET 兼容，实测恢复正常
- **引擎崩溃风暴熔断**：连续崩溃 6 次后停止无限自动重启，界面弹提示引导排查（安全软件拦截/点击播放重试）；不再后台空转

**工程**
- 引擎托管异常自动写入崩溃日志（engine-crash.log），并随诊断包导出

## V3.5.15（2026-09-22）

**新功能**
- **无缝播放（Gapless，默认开）**：切歌保持输出流不重建，间隙从秒级缩到毫秒级（现场专辑/古典连篇必备）；设置中心 → 播放可关，交叉淡入>0 时优先
- **ReplayGain 标签直读**：曲目自带 RG 标签时响度均衡直接用标签值，免 ebur128 后台分析（更快、更准）
- **重采样质量档位**：设置中心 → 播放新增 标准 / 高质量（64 阶滤波），仅引擎重采样场景生效
- **歌词偏移微调**：AM 歌词 ⚙ 浮层新增 −/＋/↺（±0.5s 步进，按曲目记忆），AM 面板、FB2K 侧栏、桌面歌词三处同步生效
- **AM 待播清单可编辑**：沉浸页/迷你待播清单支持拖拽调整顺序、✕ 移除曲目
- **迷你模式一键置顶 📌**：AM 与舞台/FB2K 迷你窗新增置顶开关（默认置顶，可取消；偏好记忆）
- **首次启动引导**：新用户三步向导（选界面主题 → 添加音乐文件夹 → 完成），老用户不受影响

**修复**
- **设备列表"（无可用设备）"**：引擎 RPC 老 bug（结果对象被 Task 二次包装），导致设备列表/输出健康/探测等所有取返回值的调用全部失效，现已根治
- **播放失败自动兜底**：输出设备打开彻底失败（如 USB DAC 断开）时自动回退 WASAPI 共享输出并弹提示，不再"点播放没反应"
- 设置页 EQ 与悬浮 EQ 面板合并为同一状态源（此前两套独立实现会互相覆盖、部分预设失效）
- 渲染层 JS 错误自动收集进诊断包（renderer-errors.txt），报障不再靠截图猜
- 清理：index.html 重复的定位按钮、未接线的快捷键定义表

**移除**
- 任务栏悬停缩略图播放按钮：Windows 11 不显示（Electron 返回值正常但系统不渲染），按实际效果砍掉

**工程**
- CI 新增冒烟测试：打包后自动验证引擎 RPC + ffmpeg + 解码探测，"能装不能播"类事故发版即拦截（`npm run smoke` 本地可跑）
- README 重写为产品页（功能矩阵 / FAQ / 构建指南）

## V3.5.14（2026-09-22）

**新功能**
- **在线音乐包（AM 主题）**：搜索历史自动记忆（输入框下拉提示，最多 15 条）；搜索/榜单/歌单结果新增「⬇ 下载已加载 N 首」批量下载（逐首顺序下载，状态行实时进度）；歌单广场新增「导入歌单」——粘贴五大平台歌单链接或纯数字 ID 自动识别平台直达详情
- **粒子舞台底栏新增播放模式按钮**：点击循环切换五种模式（与 AM 顶栏一致），不再只能去设置里改
- **托盘菜单补全**：右键托盘图标可直接 播放/暂停、上一首/下一首、迷你模式、桌面歌词

**修复**
- 桌面歌词快捷键 Alt+L 实际未接线（界面文案一直宣称支持），现已生效
- FB2K 菜单「停止 Ctrl+S」快捷键未绑定，现已生效
- 命令面板 EQ 预设「bass/vocal」调用了不存在的方法静默失效，现已修复并补齐 bass/vocal/treble 三条 EQ 曲线

**工程化（用户无感）**
- 新增 `npm run setup:tools`：自动下载 ffmpeg、校验工具链，防止再次"漏包无法播放"
- 新增 GitHub Actions 云端构建发布：push 版本标签即自动编译引擎、打包、发布 release
- sacd_extract 小工具纳入 git 管理，新机器/云端克隆即可用

## V3.5.13（2026-09-21）

**修复**
- **紧急修复：完全无法播放**。3.5.12 安装包漏带 ffmpeg/ffprobe（解码硬依赖），点播放直接失败。本版恢复随包分发，发版脚本新增前置检查防止再次漏包
- 提示：若输出设备选了 ASIO（如 HiBy USB DAC）而设备未连接，请在设置中心 → 音频输出 切换为 WASAPI 或接上设备

## V3.5.12（2026-09-21）

**优化**
- **浅色模式焕新（AM 主题 + 粒子舞台"白昼"配色）**：背景从纯白改为柔和暖白，久看不刺眼；正文/次级文字全面加深、边框对比增强，小字更易识别；AM 封面氛围背景与沉浸播放页亮度重调，无封面时沉浸页自动切换深色文字；迷你模式同步暖白
- 设置中心出品信息更新：安妮播放器（无敌章鱼哥制作出品，交流Q群1023637098）

## V3.5.11（2026-09-21）

**新功能**
- **效果器链体验小包**：VST 插件启用/关闭/打开原生界面按 20ms 平滑过渡，异常块自动回滚干声；效果器链显示每插件耗时，连续高负载自动旁通；移动插件顺序不再重建实例
- **效果器方案进阶**：当前链可导出 `.anniefx.json`，也可导入到预设列表；新增 A/B 快速对比，在当前链与快照间一键切换
- **输出健康**：设置中心 → 音频输出 新增输出健康卡片，显示设备/独占共享、源率→实际率/位深/声道、是否重采样、缓冲水位、欠载次数/帧数、限幅块数与解码失败状态
- **批量标签编辑**：FB2K 主题多选歌曲后右键“编辑标签…”或菜单栏“编辑 → 批量编辑标签…”；字段勾选后才写入，不同值显示 `<多个值>`，未勾选/留空保留原标签；封面可批量写入；曲目号/碟号默认不批量

**修复**
- 清理 V3.5.10 调试残留通知；优化效果器链关闭/排序/打开原生界面时的爆音与卡顿风险

## V3.5.10（2026-09-20）

**修复**
- VST3 原生界面打不开/卡死：IVGI2 这类 processor/controller 分离插件要求模块、插件实例、编辑器视图在同一个 STA UI 线程创建；且同插件存在活动音频实例时第二个实例 Attach 会卡死。现在打开原生界面时临时把该效果器从音频链摘掉，在编辑器 UI 线程创建独立实例，关闭后自动接回音频链并恢复参数状态。

## V3.5.9（2026-09-20）

**新功能**
- **VST 效果器方案**：设置中心 → 效果器，可把当前整条 VST3 链（插件/顺序/启停/参数状态）存为方案，一键切换/删除；缺失插件自动跳过不中断，最多保留 30 套
- **曲库标签编辑器**：FB2K/AM 列表右键“编辑标签…”，支持标题/艺人/专辑/专辑艺术家/流派/年份/曲目号/碟号/作曲家/发行方/注释与封面；留空字段保存时删除对应标签；写回走 ffmpeg 流复制不重编码（CUE/ISO 分轨不支持）；保留“在线匹配歌词/封面”入口

**修复**
- 在线更新“无法关闭/装不上”：关窗默认完整退出（设置中心可开“关闭主窗口后驻留系统托盘”恢复旧行为）；安装前等待引擎进程退出、终止扫描 Worker、销毁托盘，quitAndInstall 后 3.5s 强退兜底；更新弹窗选“稍后”后关闭播放器自动安装

## V3.5.8（2026-09-17）

**新功能**
- **全局快捷键**：软件在后台/最小化也能控制——播放暂停 Ctrl+Alt+Space、上一首/下一首 Ctrl+Alt+←/→、音量 Ctrl+Alt+↑/↓，支持键盘媒体键；设置中心 → 常规 可关闭
- **任务栏缩略图播放控制**：鼠标悬停任务栏图标，缩略图上直接点上一首/播放暂停/下一首；窗口标题与托盘提示同步显示当前曲目
- **播放定时扩展至在线播放**：定时 N 分钟停止 / 播完当前列表停止 / 单曲循环 N 遍，在线歌曲同样生效（此前仅本地播放）
- **在线音乐一键下载**：搜索/排行榜/歌单的每首歌行尾新增 ⬇ 下载按钮，实时进度显示，自动写入标题/封面/歌词标签
- **歌单收藏**：歌单详情页 ☆ 收藏，歌单广场顶部"收藏的歌单"快捷区一键直达，重启保留
- **媒体库文件夹监听**：往媒体库文件夹拷入/删除歌曲，约 3 秒后曲库自动刷新，无需手动重扫
- **重复歌曲清理**：设置中心 → 曲库工具，按"标题+艺人"查重分组，默认保留体积最大的文件，勾选后移入回收站（可恢复）

**修复**
- 托盘菜单"下一首/上一首"在播放在线歌曲时错播本地队列的问题

## V3.5.7（2026-09-17）

**新功能**
- **VST3 效果器宿主**：安妮正式成为 VST3 宿主！设置中心 →「效果器」页可扫描系统已安装的 VST3 插件或手动添加 .vst3 文件，支持启用/旁通、上下排序、移除；插件在引擎音频链中实时处理（均衡器之前），对全部界面和输出方式生效
- **插件原生界面**：播放中点槽位的 🖥 按钮，弹出插件自己的官方界面窗口（如 iZotope Insight 2 的完整表头），与 FB2K 同款体验；在插件界面里调整的参数关闭时自动保存
- **参数面板**：点 🎛 展开插件全部参数滑条，播放中实时调节实时生效
- 插件崩溃自动旁通（黄色 ⚠ 标记 + 通知），不影响播放，重新勾选即可复活；槽位配置与插件状态本地持久化，重启自动恢复整条效果器链

**底层**
- 音频引擎升级 .NET 9 + NAudio 3.1.0 全家桶（VST3 宿主必需；顺带修复新旧 NAudio 混用导致 WASAPI 独占初始化被拒的问题）

## V3.5.6（2026-09-17）

**新功能**
- 15 段均衡器（32Hz–16kHz，±12dB）：引擎 PCM 域实时处理，拖动即时生效不破音，WASAPI 独占 / ASIO 同样有效；内置平直/流行/摇滚/古典/人声/低音增强/高音增强 7 个预设，手动调节自动存为"自定义"
- AM 界面顶栏新增「≣」均衡器快捷入口；FB2K「播放」菜单新增「均衡器…」——均直达设置中心播放页
- 桌面歌词支持逐字（卡拉OK）填充：本地带 A2 增强标签的 .lrc 逐词金色填充，跟随全局逐字总开关，无逐字数据自动退回整行显示

## V3.5.5（2026-09-17）

**新功能**
- 「在线音乐」发现页：在线搜索升级为 搜索/排行榜/歌单广场 三页签，五平台榜单与歌单广场，单击即播
- 新安装默认界面为 Apple Music 主题（原粒子舞台）

**修复**
- 修复设置中心 ASIO 设备下拉空白不显示
- 修复自动更新安装时反复提示"无法关闭"（优雅退出 + 强制退出双保险，退出前统一停止音频引擎）

## V3.5.4（2026-09-17）

- 首次完整跑通自动更新链路（V3.5.3 → V3.5.4 差量更新）

## V3.5.3（2026-09-17）

**新功能**
- 扩展页上线音源插件：支持导入洛雪自定义音源脚本（.js），播放/歌词/封面优先走音源，音质标签显示音源名
- 更新弹窗显示更新公告；设置中心 → 更新与关于 可查看更新日志与下载进度，有新版本时设置齿轮亮红点

**修复**
- 输出设备失效自动回退系统默认设备（修复"WASAPI 设备不存在"报错；ASIO 驱动失效自动回退 WASAPI）

## V3.5.2（2026-09-16）

- 修复 SVLX 模式下自动更新从未运行的问题（安装版从此才能真正收到更新推送）

## V3.5.1（2026-09-16）

**新功能**
- 全新设置中心：主窗口覆盖层、十页左侧导航 + 顶部设置搜索框（Ctrl+, 呼出），Apple Music 磨砂玻璃风格
- 新增设置项：输出设备/模式切换、WASAPI 独占开关、默认播放模式、逐字歌词总开关（三主题通用）、翻译行开关、AM 歌词字号/行距/每行词数、在线匹配默认保存项、媒体库文件夹管理
- 「更新与关于」页：版本号、手动检查更新、项目链接、贡献者鸣谢
- 「扩展」功能页占位（插件生态预告）
