# 安妮播放器 V3.5.9 版本更新说明

**安妮播放器融合版 V3.5.9** —— VST 效果器方案、曲库标签编辑器，并彻底修复在线更新“无法关闭/装不上”的退出链路问题。

> 安装包：`annie-player-svlx-setup-3.5.9.exe`（Windows x64，NSIS）
> 已装 V3.5.1 及以上版本的用户：**无需下载完整包**，软件启动后自动接收差量更新，重启即完成。
> 用户数据目录与历代 V3.x 完全一致，覆盖安装不丢曲库与设置。

---

## 一、VST 效果器方案（整套链一键切换）

- 设置中心 → 效果器新增“效果器方案”：把当前整条 VST3 链保存为一个方案，包含插件、顺序、启停状态和参数快照。
- 一键切换不同场景（如“音箱模式 / 耳机模式”），切换时缺失插件会自动跳过，不中断其余插件重建。
- 方案可删除，最多保留 30 套；保存前会自动收编各槽位最新参数状态。

## 二、曲库标签编辑器

- FB2K / AM 主题列表右键新增“编辑标签…”：标题、艺人、专辑、专辑艺术家、流派、年份、曲目号、碟号、作曲家、发行方、注释，共 11 个字段，并支持本地选择封面写入。
- 留空字段保存时会从文件中删除对应标签；写回走 ffmpeg 流复制，不重编码音频。
- CUE/ISO 分轨不支持直接写回；弹窗内保留“在线匹配歌词 / 封面…”快捷入口。

## 三、在线更新退出链路修复

- 修复关窗后进程驻留导致 NSIS 升级时“无法关闭”，以及 `autoInstallOnAppQuit` 等不到退出导致已下载更新装不上的问题。
- 默认改为关窗即完整退出；设置中心 → 界面新增“关闭主窗口后驻留系统托盘”，需要旧行为可手动开启。
- 安装更新前会等待音频引擎真正退出、终止扫描 Worker、销毁托盘，`quitAndInstall` 后保留 3.5 秒强退兜底。
- 更新弹窗选择“稍后”后，关闭播放器时会自动完成安装。

---

*章鱼科技 · 无敌章鱼哥 | 更多 HiFi 资源，加入 Q 群 1023637098 获取*
