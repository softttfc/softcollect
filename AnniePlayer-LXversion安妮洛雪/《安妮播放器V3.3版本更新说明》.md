# 安妮播放器 V3.3 版本更新说明

**安妮播放器融合版 V3.3**（内部版本 1.3.3）—— Apple Music 主题交互大更新：沉浸式播放界面、迷你模式、播放模式与播放定时；另含 AM 窗口化与 FB2K 频谱的一批修复。

> 安装包：`安妮播放器融合版V3.3-setup.exe`（Windows x64，NSIS）
> 用户数据目录与 V3 / V3.1 / V3.2 完全一致，覆盖安装不丢曲库与设置。

---

## 一、新增：AM 沉浸式播放界面（⤢）

Apple Music 主题顶栏新增「⤢」按钮，一键进入沉浸式播放界面，再次点击右上角「⤡」退出。

- **封面氛围背景**：以当前封面做全屏大半径高斯模糊 + 提饱和 + 压暗（亮主题为浅晕），封面切换时 0.6s 淡入淡出；无封面时背景自动隐藏，不残留上一首封面。
- **大封面与信息**：居中巨型封面（圆角 + 投影），下方依次显示标题、艺人 — 专辑、音质参数（与顶栏红色徽章同步）。
- **可点进度条**：点击进度条任意位置直接跳转，左右分别为已播时长与剩余时长。
- **控制排**：音量滑杆、上一首 / 播放暂停 / 下一首，以及两个功能图标：
  - **💬 歌词**：点击展开右侧大字歌词列（当前行高亮放大、邻行半透、远行渐隐模糊，点击任意句可跳播）；再点收起。
  - **☰ 待播清单 / 历史记录**：右侧滑出抽屉，两个标签页可切换（详见下文）。

## 二、新增：AM 迷你模式（🗕）

顶栏「🗕」按钮把主窗口收成 360×170 的迷你条（自动置顶），迷你界面内「⤢」退出并还原原窗口尺寸与位置（位置记忆与粒子舞台迷你模式共享）。

- 迷你条内含：封面、标题 / 艺人 / 音质参数、可点进度条、音量、上一首 / 播放暂停 / 下一首，以及窗口最小化 / 退出迷你 / 关闭按钮。
- **💬 歌词**：展开歌词面板，窗口自动拉高到 360×560。
- **☰ 待播清单 / 历史记录**：展开列表面板（与歌词面板互斥），窗口自动拉高。
- 切换到其他主题时自动退出迷你模式，避免小窗装不下别的界面。

## 三、待播清单 / 历史记录面板（沉浸与迷你共享）

- **待播清单**：本地播放时显示当前播放队列（从正在播放的一首开始，高亮当前行）；在线播放时显示洛雪搜索结果队列。点击任意行直接切歌。
- **历史记录**：按播放统计的最近播放时间倒序列出本地曲目（最多 50 条），点击即以全库顺序从该曲开始播放。

## 四、新增：播放模式（AM 顶栏循环切换）

顶栏新增播放模式按钮（图标随模式变化），点击循环切换五种模式：

| 图标 | 模式 | 说明 |
|---|---|---|
| ⇥ | 全文件顺序播放 | 仅在「歌曲」全库视图播放时生效；其他列表播放时按列表内顺序 |
| → | 列表内顺序播放 | 默认行为，播到列表末尾停止 |
| 🔀 | 随机播放（全库） | 从整个媒体库随机取曲 |
| ⤨ | 列表内随机播放 | 在当前队列内随机取曲 |
| 🔂 | 单曲循环 | 当前歌曲无限循环 |

**仅对本地播放生效**：在线（流媒体）播放的续播逻辑完全不受播放模式影响。

## 五、新增：播放定时（AM 顶栏 ⏲）

点击顶栏「⏲」弹出播放定时菜单，三种选项（同时只生效一个，可随时关闭）：

1. **播完当前列表即停止**：当前队列播到末尾自动停止并提示。
2. **定时停止**：设定 N 分钟（1–720），到点自动暂停并提示。
3. **单曲循环 N 遍后停止**：设定遍数（1–99），当前歌曲循环播够遍数即停。**生效期间会临时覆盖顶栏播放模式**（相当于临时单曲循环），次数耗尽自动还原并提示。

**仅对本地播放生效**：到点时若正在播放在线歌曲，定时不执行并提示。

## 六、修复

- **AM 定位当前歌曲**：大列表（窗口化渲染）下点 🎯 不再跳回顶端，直接定位到播放行并闪烁高亮。
- **AM 切歌跳顶 / 列表空白**：双击切歌不再把列表顶回第一行；修复切歌后行内容短暂空白、需滚一下才恢复的问题（关闭滚动锚定 + 重绘后主动对齐可视区）。
- **FB2K 频谱柱不显示**：此前频谱分析只在粒子舞台主题触发，现 FB2K 主题且频谱开启时也会触发（AM 与在线播放仍不跑分析，不浪费带宽与 CPU）。
- **FB2K 右栏详情收起**：新增「详情 ▾」开关，收起后只留封面 + 参数简显 + 歌词 + 频谱，状态持久化。
- **沉浸 / 迷你首次打开显示错乱**：沉浸层增加不透明底色兜底与模糊层裁剪；迷你进入后自动校准基准窗口尺寸。

---

*章鱼科技 · 无敌章鱼哥 | 更多 HiFi 资源，加入 Q 群 1023637098 获取*
