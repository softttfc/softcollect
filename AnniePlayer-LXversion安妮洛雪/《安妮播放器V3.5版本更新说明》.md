# 安妮播放器 V3.5 版本更新说明

**安妮播放器融合版 V3.5**（内部版本 1.3.5）—— 歌词体验大版本：三主题逐字卡拉OK、歌词折行大字体、歌词外观自定义。

> 安装包：`安妮播放器融合版V3.5-setup.exe`（Windows x64，NSIS）
> 已装 V3.3.1（1.3.4）的用户：**无需下载完整包**，软件启动后自动接收几 MB 差量更新，重启即完成。
> 用户数据目录与历代 V3.x 完全一致，覆盖安装不丢曲库与设置。
> 本版本特别感谢 **电狗（@chenhaochen66）** 的 PR #2 贡献。

---

## 一、三主题逐字歌词（卡拉OK逐词扫过）— PR #2 by 电狗

逐字/逐词歌词（增强 LRC `[行时间]<词时间>文字` 与洛雪 lxlyric `<相对ms,时长ms>` 两种格式均支持）：

- **粒子舞台**：解析词时间标签后交由舞台原生逐字引擎接管，效果与舞台自带卡拉OK一致
- **FB2K**：右栏歌词逐词点亮（未唱淡 / 已唱高亮），扫过前缘渐变淡出，亮/暗色分别调优
- **AM**：主歌词面板 / 沉浸式 / 迷你小窗三处同一套逐词渲染
- 普通歌词（无词时间标签）完全不受影响，行为与之前一致

## 二、逐词架构：长行折行，不再缩字（V3.5 重构）

- 卡拉OK渲染从"整行裁切"重构为**逐词 span**：每个词独立点亮，词与词之间**自然折行**——长歌词行自动换行显示，**字号全程保持原大**（V3.3.x 时代长行只能缩小字号塞一行）
- 兜底：单个无空格的超长单词仍会按比例缩小适配
- 渲染负担反而更轻：不再需要 canvas 逐行测字宽

## 三、歌词外观自定义（AM）

鼠标移到 AM 歌词面板，右上角浮出 **⚙** 按钮，弹层提供三个滑杆（实时生效、重启保留、主歌词/沉浸/迷你同步）：

1. **字号**：70% – 160%
2. **行距**：1.2 – 2.2
3. **每行词数**：0 = 按容器宽度自然折行（默认）；设为 N 则每 N 个词强制断行（中文逐字歌词按字计）

窗口尺寸变化时歌词行会 250ms 防抖自动重新适配。

## 四、沉浸/迷你歌词滚动条：只在你需要时出现

沉浸模式与迷你小窗的歌词滚动条改为**仅滚动时显示**，停止滚动约 0.8 秒后自动隐藏；歌词区底部的横向滚动条已根除（横向溢出改为折行）。

## 五、歌词链路修复

- **内嵌歌词读取修复**：修复开发环境读取内嵌歌词（FLAC LYRICS / MP3 USLT）找不到 ffprobe 的问题（打包版用户不受影响，此修复主要保证后续版本质量）
- **lxlyric 行格式支持**：`[起始ms,时长ms]` 行级时间戳（QQ 系内嵌歌词常见格式）此前整首解析失败，现三个主题均可正常解析并逐词高亮

## 六、差量自动更新（V3.3.1 起）

本次起，自动更新通道正式运转：已装 1.3.4 的用户将收到 V3.5 的差量推送（仅几 MB），弹窗确认后重启即完成；新用户直接用完整安装包。

---

*章鱼科技 · 无敌章鱼哥 | 更多 HiFi 资源，加入 Q 群 1023637098 获取*
*特别鸣谢：电狗（@chenhaochen66）——本版逐字歌词核心贡献者*
